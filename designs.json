python -c '
import os, json
files = sorted([
    f for f in os.listdir("public/designs")
    if f.lower().endswith((".png", ".jpg", ".jpeg", ".svg", ".webp"))
])
with open("designs.json", "w") as f:
    json.dump(files, f, indent=2)
print(f"✅ Created designs.json with {len(files)} files")
'
